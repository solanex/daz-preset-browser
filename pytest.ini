# importlib mode keeps pytest from importing the addon package itself
# (whose __init__.py needs bpy) while collecting tests/.
[pytest]
addopts = --import-mode=importlib
testpaths = tests/test_scanner.py
